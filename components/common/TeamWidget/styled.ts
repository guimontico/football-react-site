import styled from "styled-components";

export const Section = styled.section`
	margin: 0 0 15px 0;
	min-height: auto;
`;

export const TeamWidget = styled.div`
	border-width: 1px;
	border-color: rgba(0, 0, 0, 0.15);
	border-style: solid;
	border-radius: 8px;
	padding: 10px;
	background: rgb(255, 255, 255);
	width: "100%";
`;

export const TeamWidgetIframe = styled.iframe`
	width: 100%;
	border: none;
	transition: all 300ms ease;
`;
